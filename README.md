# Website Link Analyzer - Анализатор битых ссылок

Комплексный инструмент для анализа веб-сайтов на наличие битых ссылок. Скрипт обходит весь сайт, собирает все ссылки и проверяет их доступность, создавая подробные отчеты.

## Особенности

- 🔍 **Полный обход сайта** - автоматически находит и обрабатывает все страницы сайта
- 🔗 **Множественные типы ссылок** - находит ссылки в различных HTML-тегах и в тексте
- 🌐 **Поддержка внешних ссылок** - проверяет как внутренние, так и внешние ссылки
- 🔒 **Надежная работа с HTTPS/SSL** - корректно обрабатывает SSL-сертификаты
- 🤖 **Уважение к robots.txt** - следует правилам robots.txt (опционально)
- ⚡ **Многопоточность** - параллельная проверка ссылок для быстродействия
- 📊 **Детальные отчеты** - генерирует отчеты в JSON, CSV и текстовом формате
- 🚦 **Умная обработка ошибок** - корректно обрабатывает различные типы ошибок HTTP

## Установка

### Требования
- Python 3.7+
- Необходимые пакеты (автоматически установятся)

### Установка зависимостей
```bash
pip install requests beautifulsoup4 lxml urllib3
```

## Использование

### Базовое использование
```bash
python link_analyzer.py https://example.com
```

### Расширенные параметры
```bash
python link_analyzer.py https://example.com --max-pages 50 --delay 0.5 --max-workers 10
```

### Полный список параметров
```bash
python link_analyzer.py --help
```

## Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `url` | URL сайта для анализа (обязательный) | - |
| `--max-pages` | Максимальное количество страниц для обхода | 100 |
| `--delay` | Задержка между запросами (секунды) | 1.0 |
| `--timeout` | Таймаут для HTTP запросов (секунды) | 10 |
| `--max-workers` | Количество потоков для проверки ссылок | 5 |
| `--output` | Директория для сохранения отчетов | текущая |
| `--no-robots` | Игнорировать robots.txt | false |
| `--user-agent` | Кастомный User-Agent | Chrome UA |

## Примеры использования

### 1. Быстрый анализ небольшого сайта
```bash
python link_analyzer.py https://mysite.com --max-pages 20 --delay 0.5
```

### 2. Глубокий анализ с сохранением в отдельную папку
```bash
python link_analyzer.py https://mysite.com --max-pages 200 --output ./reports
```

### 3. Анализ с игнорированием robots.txt
```bash
python link_analyzer.py https://mysite.com --no-robots --max-workers 10
```

### 4. Анализ с кастомным User-Agent
```bash
python link_analyzer.py https://mysite.com --user-agent "MyBot/1.0"
```

## Что анализирует скрипт

### Типы ссылок
Скрипт находит и проверяет ссылки в следующих элементах:

**HTML теги и атрибуты:**
- `<a href="">` - обычные ссылки
- `<link href="">` - CSS и другие ресурсы
- `<img src="">` - изображения
- `<script src="">` - JavaScript файлы
- `<iframe src="">` - встроенные фреймы
- `<form action="">` - действия форм
- `<video src="">`, `<audio src="">` - медиа файлы
- И многие другие...

**Текстовые ссылки:**
- URL'ы найденные в тексте страницы (например, в статьях)

### Типы проверок
- **Внутренние ссылки** - ссылки на том же домене
- **Внешние ссылки** - ссылки на другие домены
- **HTTP статус коды** - все коды ответа (200, 404, 500, и т.д.)
- **Редиректы** - отслеживание цепочек перенаправлений
- **SSL сертификаты** - корректная обработка HTTPS
- **Время ответа** - измерение скорости ответа

## Отчеты

Скрипт генерирует три типа отчетов:

### 1. JSON отчет (`link_analysis_YYYYMMDD_HHMMSS.json`)
Полный отчет со всей информацией:
```json
{
  "scan_info": {
    "base_url": "https://example.com",
    "scan_date": "2024-01-15T10:30:00",
    "pages_scanned": 50,
    "total_links_found": 1250,
    "unique_links": 800,
    "broken_links_count": 15
  },
  "broken_links": [...],
  "all_links_status": {...}
}
```

### 2. CSV отчет (`broken_links_YYYYMMDD_HHMMSS.csv`)
Таблица с битыми ссылками для анализа в Excel:
- source_page - страница где найдена ссылка
- url - адрес битой ссылки
- status_code - HTTP код ответа
- error - описание ошибки
- response_time - время ответа

### 3. Текстовый отчет (`link_analysis_report_YYYYMMDD_HHMMSS.txt`)
Читаемый отчет с группировкой по страницам:
```
ОТЧЕТ ОБ АНАЛИЗЕ ССЫЛОК
==================================================

Сайт: https://example.com
Дата сканирования: 2024-01-15 10:30:00
Обработано страниц: 50
Найдено уникальных ссылок: 800
Найдено битых ссылок: 15

БИТЫЕ ССЫЛКИ:
------------------------------

Страница: https://example.com/page1
  - https://broken-site.com/page404
    Статус: 404 - Not Found
    Тип: external
```

## Логирование

Скрипт ведет подробные логи в двух местах:
- **Консоль** - текущий прогресс и основная информация
- **Файл** `link_analyzer.log` - детальная информация и ошибки

## Обработка ошибок

Скрипт корректно обрабатывает различные ситуации:

- ❌ **404 Not Found** - страница не найдена
- ❌ **403 Forbidden** - доступ запрещен
- ❌ **500 Internal Server Error** - ошибка сервера
- ❌ **Timeout** - превышение времени ожидания
- ❌ **SSL Errors** - проблемы с сертификатами
- ❌ **Connection Errors** - проблемы с подключением
- ❌ **Too Many Redirects** - циклические редиректы

## Производительность

### Рекомендации по настройке:

**Для небольших сайтов (< 50 страниц):**
```bash
--max-pages 50 --delay 0.5 --max-workers 5
```

**Для средних сайтов (50-200 страниц):**
```bash
--max-pages 200 --delay 1.0 --max-workers 8
```

**Для больших сайтов (> 200 страниц):**
```bash
--max-pages 500 --delay 1.5 --max-workers 10
```

### Важные замечания:
- Увеличение `max-workers` ускоряет проверку ссылок, но может создать нагрузку на сервер
- Уменьшение `delay` ускоряет обход, но может привести к блокировке IP
- Некоторые сайты могут ограничивать количество запросов в минуту

## Этика использования

При использовании скрипта следуйте принципам этичного веб-скрапинга:

- ✅ Используйте разумные задержки между запросами
- ✅ Уважайте robots.txt (если не указан `--no-robots`)
- ✅ Не создавайте чрезмерную нагрузку на серверы
- ✅ Используйте для анализа собственных или разрешенных сайтов

## Устранение неполадок

### Проблема: SSL ошибки
**Решение:** Скрипт автоматически игнорирует SSL ошибки, но если проблемы продолжаются, проверьте сетевые настройки.

### Проблема: Блокировка по IP
**Решение:** Увеличьте параметр `--delay` или используйте кастомный `--user-agent`.

### Проблема: Медленная работа
**Решение:** Увеличьте `--max-workers` и уменьшите `--delay`, но следите за нагрузкой на сервер.

### Проблема: Недостаточно памяти
**Решение:** Уменьшите `--max-pages` или обрабатывайте сайт частями.

## Лицензия

Этот скрипт предоставляется "как есть" для образовательных и коммерческих целей. Используйте ответственно и в соответствии с правилами веб-сайтов.

## Поддержка

При возникновении проблем:
1. Проверьте логи в файле `link_analyzer.log`
2. Убедитесь в корректности URL
3. Проверьте сетевое соединение
4. Попробуйте с меньшими значениями параметров